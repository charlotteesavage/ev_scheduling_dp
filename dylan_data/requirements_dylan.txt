# Requirements for running Dylan's CPLEX notebook (solution_analysis.ipynb)

# Core scientific computing
pandas>=1.3.0
numpy>=1.21.0
matplotlib>=3.4.0
scipy>=1.7.0

# Jupyter environment
jupyter>=1.0.0
ipykernel>=6.0.0

# Geospatial and utilities
geopy>=2.2.0
seaborn>=0.11.0

# Google Maps API (optional - for travel time computation)
googlemaps>=4.5.0

# Parallel processing (used in notebook)
ray>=1.9.0

# CPLEX solver - CHOOSE ONE:
# Option 1: IBM CPLEX Python API (requires CPLEX installation + license)
# docplex>=2.23.0

# Option 2: If CPLEX is installed locally, install from local path:
# See: /Applications/CPLEX_Studio*/cplex/python/3.*/[platform]/
# Run: pip install /path/to/cplex/python/[version]/[platform]

# Note: Without CPLEX, you can still view the notebook and understand
# the data structure, but cannot run the optimization.
